1st step 

| Stage | What your code does | Technology/concept |
|---|---|---|
| API setup | Runs a backend at `localhost:8000` | FastAPI, Uvicorn |
| Health check | Confirms that the backend is running | `GET` endpoint |
| File upload | Accepts only CSV files | `POST`, `UploadFile` |
| Ingestion | Reads uploaded CSV content | Pandas, `DataFrame` |
| Validation | Checks expected columns, missing values, duplicates, and duplicate task IDs | Data-quality rules / data contract |
| Cleaning | Trims text, removes exact duplicate rows, fills selected optional fields | Data transformation |
| Business rule | Never invents missing `Task_ID` or `Status` values | Required vs optional fields |
| Quarantine | Separates unsafe records for manual review | Error handling and governance |
| Output storage | Saves processed and quarantine CSVs | Local file storage |
| Download | Provides API links to download saved datasets | `FileResponse` |
| Traceability | Gives every upload a unique `run_id` | UUID |

Architecture 
Upload CSV
   ↓
FastAPI receives the file
   ↓
Pandas reads it into a DataFrame
   ↓
Validate data quality
   ↓
Clean safe fields
   ↓
Quarantine unsafe records
   ↓
Save downloadable CSV outputs

we connect FastAPI to PostgreSQL using SQLAlchemy.
FastAPI → SQLAlchemy → PostgreSQL → insightflow_db

NEXT STEP IN LAYER 1

Dataset
   ↓
Profiling Engine
   ↓
Column Analysis
   ↓
Statistics
   ↓
Data Quality Metrics
   ↓
Profile Report


LAYER 1 ENDED AND WE ACHIEVED THE FOLLOWING ARCHITECTURE

CSV Upload
   ↓
FastAPI ingestion
   ↓
Pandas DataFrame
   ↓
Validation and business rules
   ↓
Cleaning safe fields
   ↓
Quarantine unsafe records
   ↓
Store data and metadata in PostgreSQL
   ↓
Download cleaned datasets
   ↓
Streamlit dashboard and analytics

LAYER 2

Layer 1 trusted data
        ↓
Business metrics
        ↓
KPI calculations
        ↓
Trend analysis
        ↓
Insights and recommendations

1. KPI Engine
2. Trend Engine
3. Insight Engine
4. Recommendation Engine
5. Analytics Repository Refactor
6. API Response Schemas
7. Executive Dashboard Refinement
8. Automated Tests
9. Documentation and Final Validation


Current Flow

Dashboard → FastAPI → AnalyticsEngine → PostgreSQL

New Flow After This Feature

Dashboard → FastAPI → KPI Service → Analytics Repository → PostgreSQL
                              ↓
                   KPI result stored in PostgreSQL


Create:
- app/analytics/kpi_service.py

Modify:
- app/analytics/engine.py
- app/main.py
- app/database/models.py

app/analytics/kpi_service.py
Calculates total tasks, completion rate, pending rate, high-priority backlog, workload by layer, and component distribution.

app/analytics/engine.py
Delegates KPI calculation to the dedicated KPI service.

app/main.py
Keeps the GET /analytics/kpis/{dataset_id} endpoint connected to the new service.

app/database/models.py
Uses the existing DatasetAnalytics table to persist KPI results.


COMMANDS
 python -m streamlit run dashboard/app.py
 python -m uvicorn app.main:app --reload